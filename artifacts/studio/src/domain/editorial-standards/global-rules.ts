export type { GlobalRule } from "./types";

/**
 * Reglas globales CloudBooks — aplicadas a todos los formatos y colecciones.
 * Importadas por la capa de generación y QA para enforcement real.
 */
export { GLOBAL_RULES } from "./data";
