export type { QAGate } from "./types";

/**
 * Gates de QA por dimensión.
 * blocksApproval === true → el gate bloquea aprobación si no se supera.
 * Fuente de verdad para la capa de QA server-side y la UI de diagnóstico.
 */
export { QA_GATES } from "./data";
