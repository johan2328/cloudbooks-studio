export type { GenerationPolicy } from "./types";

/**
 * Políticas de generación por formato.
 * Define modelos permitidos, calidad, fallbackApprovable y batchGating.
 * Usadas por la capa de generación para validar parámetros antes de ejecutar.
 */
export { GENERATION_POLICIES } from "./data";
